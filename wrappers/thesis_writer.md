Run the thesis_writer skill. Follow $CONAN_ROOT/.claude/skills/thesis_writer.md steps 1–9 verbatim, including the step 6.5 honest-decline short-circuit, the step 6.8 semantic gate (challenger pass), and the §8f dispatch table.

Outputs wired into the Conan app via the Supabase MCP (project ref: xvwvwbnxdsjpnealarkh). All persistence must actually happen on that project — thesis_jobs status transitions (queued → drafting → promoted | gate_failed_retrying | dlq); candidates UPSERT on (ticker, mic) with kill_conditions JSONB and catalyst (date, window) pair; candidate_events append with event_type ∈ {'created','thesis_drafted_by_claude'}; thesis_drafting_failures on DLQ; dossier markdown PUT to Storage bucket 'candidates/<YYYY>/<MM>/<ticker>_<signal_id>.md'. Returned JSON summarizes writes — does not replace them. If you cannot reach the Supabase MCP, do not fabricate success; report the failure.

Guardrails:

- Reset stuck 'drafting' rows >30 min back to 'queued' first (skill step 1). Then load a generous queued window (LIMIT 50) with signal + scanner config so you can rank short_positioning separately.

- Quota checks before building the batch (skill step 2): the 15/day promotion cap across thesis_writer + signal_resolver, AND the short_positioning sub-quota from scanners.config.daily_promotion_limit (default 5 for esma_short_scanner). Non-short rows stay FIFO; short rows rank by score_with_bonus DESC NULLS LAST, scan_date DESC, created_at ASC. Overflow short rows stay status='queued' with gate_reasons=['profile_deferred_short_limit'] — defer, not reject. Take at most 5 jobs total per run.

- Empty working batch → {processed: 0, deferred_short_jobs: N, empty_queue_exit: true} and stop.

- Process one row at a time, serially — both gates on the thesis_writer flow are stateful via drafted_thesis + all_drafts.

- Only Immediate-band signals produce candidates (invariant 1). thesis_jobs rows come from the reactor on band_with_bonus='immediate'; needs_scoring / scoring rows belong to signal_resolver, don't touch them.

- Honest-decline short-circuit (§6.5): if draft has confidence:'low' OR insufficient_signal:true, DLQ immediately with final_reasons=['routine_declined: <reason>']. Skip BOTH gates and skip retry. This is the preferred outcome when research can't support an asymmetry claim.

- Two gates, BOTH authoritative (invariant 2). Semantic gate (challenger routine §6.8) runs BEFORE the syntactic gate (§7). Verdict routing per §8f:
  - confirm + syntactic pass → promote (§8a).
  - confirm + syntactic fail (1st) → retry syntactic (§8b). 2nd fail → DLQ with final_reasons=['syntactic_fail', ...gate_reasons].
  - challenge (1st) → amend once addressing challenger.strongest_counter + required_fixes (§8d). (2nd challenge) → DLQ with final_reasons=['challenger_challenge_exhausted', ...].
  - kill → DLQ immediately, no retry, no syntactic gate, final_reasons=['challenger_kill', ...challenger.reasons]. Kill is terminal — widely-watched deal with no named edge, hallucinated catalyst, cosmetic kill conditions.

- Two independent retry budgets (invariant 8): attempt_count (max 2 drafts for syntactic retries) and challenge_count (max 2 challenges). Increment challenge_count BEFORE invoking the challenger (advisory runaway guard). Exceeding either → DLQ. Happy path 2 Claude calls (draft + confirm); worst DLQ 4 (2 drafts × 2 challenges).

- all_drafts shape: array of {draft, gate_verdict, challenge_verdict} triples, one per attempt. Written to candidate_events.payload.drafts on promotion AND thesis_drafting_failures.all_drafts on DLQ.

- candidate_events.event_type discipline (invariant 6): 'created' on xmax=0 (first insert), 'thesis_drafted_by_claude' on UPSERT update (convergence re-draft). Never 'thesis_updated' — that value exists in the enum but is NOT in the fanout email-trigger set, so re-drafts would send no notification.

- Catalyst date → (date, window) pair per §8a.2. Exactly one of (next_catalyst_date, next_catalyst_window) non-NULL per candidates_catalyst_exactly_one CHECK. Daterange literal: '[YYYY-MM-DD,YYYY-MM-DD]'::daterange. Quarter/half/month/early-mid-late mappings per step 8a.2.

- UPSERT sets kill_conditions, catalyst, thesis_approved_at, last_aging_evaluated_at on first creation so candidate_aging has real data from day one. Render dossier markdown via render_candidate_markdown_v2 and PUT to Storage before UPSERT (§8a.3).

- Boilerplate banned: "scanner classified signal_type", "tdnet filed", "auto-generated by", "placeholder thesis", "no thesis yet", "to be researched" auto-fail the syntactic gate. Reasoning tags ([verified] / [inferred] / [speculated]) are load-bearing — ≥5 across situation+why_underpriced+steelman, ≥1 [verified], >2 untagged load-bearing sentences = hard fail.

- Research budget ≤6 WebSearch queries (skill step 5): 1-2 primary-source confirmations, 1-2 market-context, 1-2 disconfirming. At least one web_research entry with lean ≠ 'strengthening'.

- Call assess_thesis_v2 via Bash (cd to "$CONAN_ROOT", then python3 -c with the modal_workers.shared.candidate_gate import). Do NOT inline-reimplement. The challenger is a Claude app routine — invoke via your own session and record verdict JSON verbatim in all_drafts[i].challenge_verdict.

Project context:

- Project: Conan v2
- Supabase ref: xvwvwbnxdsjpnealarkh
- Skill file on disk: $CONAN_ROOT/.claude/skills/thesis_writer.md
- Queue source: thesis_jobs WHERE status='queued' (reactor enqueues only band_with_bonus='immediate' signals by construction; signal_resolver can also promote inline without touching this queue).
- Short-positioning scanners feeding the queue: esma_short_scanner (daily_promotion_limit=5).

Report JSON: {processed, promoted, retried_and_passed, challenger_retried_and_passed, dlq_syntactic, dlq_challenger_kill, dlq_challenger_challenge_exhausted, dlq_declined, deferred_short_jobs, skipped_over_quota, empty_queue_exit}.
